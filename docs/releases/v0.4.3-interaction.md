# SyphonNov v0.4.3 交互体验更新日志

发布日期：2026-09-10  
分支：`feature/v0.4.3-interaction`  
版本：`0.4.3+1`

## 用户可见变化

- 顶栏下方新增 42px 固定节点提示条，五类节点直接读取注册表和 `kCategoryInfo`；展开的节点库使用独立浮层，不会推动或反复布局画布。
- 鼠标悬停分类平滑展开节点库，离开分类与浮层之间保留 120ms 容错；浮层以淡入缩放进入和退出，并修复了与文件/编辑/视图菜单重叠时的点击阻断。
- 节点库支持按中文名称、说明和英文节点 ID 搜索；收藏与最近使用顺序写入 `settings.json`。
- 节点可直接单击创建在视口中心，也可拖入画布。拖动反馈是分类色圆点，画布用独立光环显示合法落点；画布外、Esc 或右键取消均不改工作流。
- 放下坐标经过窗口坐标、画布局部坐标和 pan/zoom 世界坐标的统一换算。画布边缘放置会保留 12px 安全边距，可选 20 单位网格吸附。
- 新节点从圆点平滑展开成完整卡片。直接拖动节点仍逐指针更新，不对位置插值。

## 动效与无障碍

- 新增统一 `MotionTokens`，提供 90/180/260ms 三档节奏与平滑减速曲线。
- 设置面板新增“完整 / 简化 / 关闭”三档动态效果，并读取系统 `disableAnimations`。关闭后功能和键盘路径保持可用。
- 顶部分类和节点条目支持焦点、Enter/Space 与 Tab 导航；Esc 关闭搜索浮层或取消正在进行的创建。

## 性能边界

- 节点库是 Overlay，不进入主内容 Column 的展开布局。
- 拖放合法性提示由独立 `ValueNotifier`、`AnimatedBuilder` 和 `RepaintBoundary` 驱动；移动圆点时不调用 `GraphStore.notifyListeners`，也不重建节点世界。
- 成功拖放只调用一次 `addNode`，因此只有一个 undo 快照和一次 auto-run；边缘夹紧只更新布局。

## 验证

- `flutter analyze --no-pub`：0 issues。
- 全部 Flutter 测试：175/175 通过，其中新增 7 项节点条回归，覆盖五分类、固定高度、搜索、画布内外落点、关闭动效，以及 100%/125%/150% DPI 的边缘放置。
- 既有属性分栏、数据表滚轮/滚动条和工具栏菜单交互均已回归。
- Windows x64 Release 构建成功；EXE 的 FileVersion 与 ProductVersion 均为 `0.4.3+1`。
- NSIS 安装器：`dist/SyphonNov-0.4.3-Setup.exe`，13,787,935 字节，SHA-256 `CBC3E3359CA701D229713C3D0667F424C546B3601D60452FCCD2E4951CE0FF33`。
- 隔离目录静默安装、隐藏启动存活 5 秒与静默卸载均通过。

## 已知后续项

- 对齐参考线和附近节点等距提示并入 v0.6.0 的大型节点图整理能力。
- 删除节点的退出动画将在建立大图性能基线后加入，避免为了视觉过渡延迟实际删除或保留失效节点。
