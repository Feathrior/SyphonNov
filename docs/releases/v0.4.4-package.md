# SyphonNov v0.4.4 Package 与文件拖放更新日志

发布日期：2026-09-10  
分支：`feature/v0.4.3-interaction`  
版本：`0.4.4+4`

## 用户可见变化

- 顶部节点库的书脊文字改为字符直立的纵向排版；展开框按当前分类的节点数量收缩，短分类不再留下大片空白。
- Delete 与 Backspace 恢复为画布固定删除入口；用户自定义的删除快捷键作为额外入口保留。
- 从资源管理器拖入 CSV、TSV、TXT 或 XLSX 时，在投放位置自动生成“表格输入”节点，节点名称使用文件名；多文件按 32px 间距错开放置。
- 新增灰色 Package：Shift 多选节点后，可从右键菜单命名并打包。折叠时显示一个灰色代理节点，自动暴露子图边界输入/输出端口；内部子图继续参与执行，外部连线重定向到对应代理端口。
- Package 以浅灰背景和完整虚线边框标出展开区域；代理、成员节点和区域控制在展开/收起时连续过渡。
- Package 代理的整张卡片都是明确的选择与拖动区域，拖动时逐帧跟随指针；卡片和区域右上角提供直接展开/收起按钮。
- Package 右键菜单精简为“保存到 Package 库”和“解散 Package”，不再混入节点添加与展开操作。
- Package 可保存到持久化库，从顶部 Package 标签或画布右键菜单再次实例化；顶部库支持删除模板。
- 不同节点分类之间切换时，弹层宽度会平滑插值；内容切换改用轻量淡化与微缩放，并隔离重绘，消除重复模糊滤镜造成的停顿。
- 新增六向右键圆环：按住右键显示输入、初步、运算、转化、可视化和 Package 六类；沿方向滑动选择详细条目，继续外划成为圆点并在松开位置创建。未触及圆环时松开会取消。
- 修正圆环动画坐标原点，色环现在精确出现于右键按下位置；环体收窄并直接使用六类颜色，靠近时产生外扩和柔光反馈。
- 圆点从色环拉出前具有弹性阻力和连接带；跨过分离阈值时锁定分离点对应的详细节点，随后无论拖向哪个方向都不再改变类型。
- 圆环出现和圆点分离过程加入局部高斯模糊、回弹缩放和光晕；滤镜边界跟随色环与圆点，不对整张画布做模糊。
- Package 折叠代理加入液态毛玻璃结构概览，在半透明玻璃层中按原相对位置显示内部节点与内部连线。
- 输出节点缩放改由画布级指针周期接管；节点因尺寸变化重建后仍能接收后续移动和松开，支持连续多次调整。
- 设置中可分别启用上边栏、传统右键菜单和右键圆环。三者可任意组合；同时启用传统菜单与圆环时，快速右键打开列表，长按或滑动进入圆环。
- 顶部节点始终遵循注册表中的固定顺序；收藏和最近使用不再改变书脊位置。
- Package 明确显示“前置输入”和“后续输出”，端口名称同时标出内部节点与插槽，折叠前后的外部连线保持不变。
- 全局快速、标准和空间动效分别调整为 110ms、230ms 和 320ms，使状态变化更容易跟随。

## 数据与兼容性

- Package 元数据随工作流 JSON 保存；普通 v0.4.x 分组仍按原格式加载。
- Package 库保存在应用 `settings.json`，模板实例化时为节点和连线生成新 ID，避免与当前画布冲突。
- 修正 Win32 `WM_DROPFILES` 坐标被二次转换的问题，并修复 UTF-16 文件路径缓冲区末尾写入越界。

## 验证

- `flutter analyze --no-pub`：0 issues。
- 全部 Flutter 测试：198/198 通过；新增覆盖圆环原点、分离后类型锁定、连续输出缩放、Package 毛玻璃概览与实时拖动。
- Windows x64 Release 构建成功；EXE 的 FileVersion 与 ProductVersion 均为 `0.4.4+4`。
- NSIS 安装器：`dist/SyphonNov-0.4.4-Setup.exe`，13,819,481 字节，SHA-256 `135FEBA645624BE637D6DEB4788BE06161937E334174DD21AA2AAF728892D6F5`。
- 隔离目录静默安装、隐藏启动存活 5 秒与静默卸载均通过。
