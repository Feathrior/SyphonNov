# 第一轮科研正确性修复记录

基线：`0f76367618181ee5bcc407ba84ca8ea30b3d2d54`（0.3.3+1），日期：2026-09-05。工作分支：`fix/scientific-correctness`。保留上游历史和 origin，修复位于本地工作树。

原项目的节点式工作流、数据导入和自绘图元为继续开发提供了基础。实际审查发现，部分计算和坐标映射存在能改变科研结论的错误，验证与导出能力也不足。本轮优先处理可复现的正确性问题，并记录算法的条件和边界；测试覆盖范围之外的科研适用性仍需单独验证。

## 本轮修复范围

| 编号 | 原问题 | 现在的行为 | 详细记录 |
| --- | --- | --- | --- |
| N01 | 幂运算优先级、结合顺序及表达式解析错误 | 严格解析完整表达式；支持右结合幂、负指数、带符号科学记数法；非法输入明确失败 | [数值修复](fixes/numerics.md) |
| N02 | 内部结果固定保留六位小数，小量数据被清零 | 计算和节点传递保留 double；显示格式不再反向改变数据 | [数值修复](fixes/numerics.md) |
| N03 | 非均匀网格差分错误、重复 X 伪造分母、空数据越界 | 非均匀三点内点差分；校验严格递增的有限 X；积分使用梯形法与补偿累加 | [数值修复](fixes/numerics.md) |
| N04 | 拟合数值不稳定、线性模型实为二次、指数中间量溢出 | 中心缩放 Householder QR；线性固定一阶；指数预测使用中心化对数模型；秩不足和模型定义域错误明确拒绝 | [数值修复](fixes/numerics.md) |
| N05 | 随机抽样无法复现、改导出页面重新生成数据 | 抽样增加 seed；图表样式、页面、DPI 及其撤销/重做保留原始计算结果 | [数值修复](fixes/numerics.md)、[页面流程](fixes/export-workflow.md) |
| P01 | 主图与叠加图元各用独立坐标范围 | 共享数据到页面的映射；无数值坐标轴的图明确提示不支持数值叠加 | [绘图修复](fixes/plotting.md) |
| P02 | 非均匀 X 被等距排列，微小范围被压缩，静默截取数据 | 使用真实数值 X；移除带量纲的固定 epsilon；取消既有行列截断；缺失行断线，负柱与热力颜色范围修正 | [绘图修复](fixes/plotting.md) |
| E01 | 仅 PNG，导出排版随预览窗口变化 | 固定毫米画布；SVG/PDF 保留路径、文字与渐变；PNG 带 DPI 元数据；异常参数可恢复且阻止错误导出 | [导出实现](fixes/export.md)、[页面流程](fixes/export-workflow.md) |
| E02 | 长热力图列名、火山图轴名裁切，三维刻度文字重叠 | 根据真实文字尺寸预留边距；三维轴字号按逻辑像素使用，文字碰撞时保留刻度线并减少标签 | [导出排版检查](fixes/export.md) |
| U01 | 窄窗口帮助内容溢出，表格滚动条依赖平台 | 帮助可滚动；显式双轴滚动条；测试真实拖动与内容可达性 | [界面修复](fixes/ui-scrollability.md) |
| D01 | 构建说明与实际 SDK 约束不一致，缺少验证流程 | README 对齐 Dart ^3.12.2，准备本地 SDK、验证脚本和 Windows CI；兼容 Windows PowerShell 5.1 的中文脚本编码 | 下方验证说明 |
| D02 | README 的 MIT 链接没有实际文件 | 记录真实来源状态，纠正失效链接，未代替原作者授予许可 | [来源说明](UPSTREAM.md) |

## 四个原始反例：同一测试，修复前后实跑

`test/review_regressions_test.dart` 只调用基线已有接口。同一份文件在独立基线工作树 `.syphon-tools/baseline` 和修复副本中分别运行。

| 输入 | 原版实际结果 | 修复后结果 |
| --- | --- | --- |
| `2*x^2`，`x=3` | 36 | 18 |
| `0.0000001*x`，`x=1` | 0 | 保留 `1e-7` |
| `y=x²`，节点 `x=0,1,3`，在 `x=1` 求导 | 3 | 2 |
| `(1,1),(1,2)` 求导 | 未报错并产生伪造斜率 | 明确拒绝重复 X |

基线 **4/4 失败**，修复版 **4/4 通过**。原始输出保存在 [基线日志](verification/baseline-regressions.txt)。这里是执行结果，不是仅从代码推演的预期。

额外反例包括：X 在 `10^9` 附近的多项式拟合、`diag(1e-20,2e-20)` 线性方程、X 在正负 1000 附近的指数拟合、第 5001 个散点、`1e-12` 单位缩放、长标签导出，以及改页面导致随机数据重算。详细记录给出相应输入、行为和验证方式。

## 验证方式与交付边界

验证环境为 Windows、Flutter 3.47.2 / Dart 3.13.2。Flutter SDK 后续迁移到 `%USERPROFILE%/develop/flutter`；Pub 缓存仍放在项目旁的 `.syphon-tools/pub-cache`，两者都没有复制进源码仓库。验证入口：

```powershell
./scripts/verify.ps1
# C++ 构建环境完整时：
./scripts/verify.ps1 -Build
```

脚本执行依赖解析、静态分析和完整测试。也可用 `-FlutterRoot <SDK目录>` 指定环境。本地插件使用目录联接解决符号链接权限问题，未改变系统开发者模式。两个脚本均使用 UTF-8 BOM，并已通过 Windows PowerShell 5.1 的实际语法检查。

在 Windows 自带的 PowerShell 5.1 下实际执行上述脚本，**退出码 0：依赖解析成功，静态分析无问题，整套 111/111 测试通过**。原有导入、连线执行、撤销、组件测试与新增数值、坐标、页面及导出测试均在同一轮运行。

- [完整执行日志](verification/verification-run.txt)：包含 SDK 版本、依赖解析、分析与测试。
- [最终测试日志](verification/full-tests.txt)、[最终静态分析日志](verification/static-analysis.txt)。
- [十类导出文件的结构检查](verification/export-structure.json)：实际读取生成的文件，核对 160 × 100 mm 页面；PDF 页面为 453.54331 × 283.46457 pt、没有图像对象且中文可提取；SVG 有路径和文字、没有 image；PNG 为 1890 × 1181 像素，pHYs 对应约 299.9994 DPI。

十类 PNG/SVG/PDF 样本保存在本地 `build/export-qa/`。用 Poppler 重新渲染全部十个 PDF 并目视检查，另检查修复后的 PNG；已确认的方块字、长列名裁切、火山图轴标题裁切及三维刻度文字重叠均已消除。测试用真实字体明确加载，PDF 的字体回退仍可能产生字形差异。目视验证针对这些样本，不构成任意图形密度与页面尺寸的排版保证。

日志中 `Helvetica has no Unicode support` 是 PDF 库创建标准字体时的提示；缺字失败路径和中文字体嵌入有独立测试，实际生成文件另经中文提取与渲染检查，未把提示直接等同于缺字或忽略成品验证。

2026-09-05 的 Windows 应用构建因本机缺少 MSVC、CMake 和 Windows SDK 组件退出。当日微软安装器返回 5007，要求 Windows 管理员权限；工具沙箱授权不能替代系统提权。**该环境阻碍已在 2026-09-06 解决并成功生成 Windows release 程序**，见下方续记及 [构建环境记录](verification/build-environment.txt)。真实桌面窗口交互尚未验收。

`.github/workflows/verify-windows.yml` 已添加分析、测试及构建步骤，但没有推送上游或在远程运行，因此没有把配置存在写成 CI 已通过。

## 数值分析学习记录

从 [学习入口](learning/README.md) 开始，依次学习：表达式与误差传播、非均匀差分与梯形积分、最小二乘与可复现计算、坐标映射、分辨率与矢量导出。

每篇保留最小反例、数学对象与假设、必要推导、修复选择、验证方式、失效边界和带折叠答案的练习。另有三反例对照图及其重建方法。项目 `AGENTS.md` 已把“以后遇到相关问题继续补教学记录”写为持续工作约定。

## 尚未覆盖的事项

- binary64 极限、高阶病态拟合、任意精度、条件数估计与误差条；微分端点仍为一阶，积分仍为梯形法。
- 原坐标多项式系数可能病态，当前稳定预测保留在缩放基底中；指数拟合是对数空间最小二乘。
- 示例数据生成器仍有随机源；抽样复现要求同输入、同 seed、相同实现环境，严谨实验仍应保存原数据。
- 函数间断点连线、曲线交点的几何容差与采样策略，需要另一轮专门审查。
- 等比例坐标、KDE 带宽、箱线须定义、火山图显著性阈值及多重检验流程尚未完整审定。
- SVG 接收端字体可用性、PDF 字体回退差异、路径度量近似误差和超大数据绘图成本仍有明确边界，见各专题记录。

这些是下一轮有待处理的问题，不计入本轮“已修复”。

## 2026-09-06 续记：能力审计与 C++ 环境补全

本次按用户要求检查科研工作流是否完整，并补齐本机 C++ 组件。完整判断、优先级与证据入口见 [科研绘图能力审计](科研绘图能力审计.md)。此次没有修改产品算法；新发现的缺陷登记为待修，不能计入第一轮修复成果。

### D03：本机 Windows 构建阻碍已解决

新增 `.vsconfig` 和 `scripts/install_windows_cpp.ps1`，通过微软官方安装器和标准 UAC 提权补齐 MSVC 14.44.35207、CMake 3.31.6-msvc6、Windows SDK 10.0.26100.0。安装退出码 0，无需重启。脚本校验安装器签名，记录退出结果；启动失败时更新失败记录，避免沿用上次成功状态。

`vswhere` 确认组件完整，Flutter doctor 的 Windows/Visual Studio 项通过。实际执行 `flutter --no-version-check --suppress-analytics build windows --release --no-pub`，**退出码 0，编译约 63.4 秒**。程序及依赖已归档到 `dist/SyphonNov-windows-x64-2026-09-06.zip`；[构建清单](verification/windows-build-manifest.json)记录源代码基线、工具链、文件哈希和验证边界。

桌面操作接口返回 `Windows Computer Use Sky runtime is unavailable`，因此未完成实际窗口启动与交互验收；这也不是跨机器安装包测试。远程 CI 尚未运行。

本次收尾的全项目静态分析无问题，新安装脚本通过 Windows PowerShell 5.1 语法检查；21 个程序归档文件的内容与构建目录一致且 CRC 正常。此前 111 项产品回归没有在本次审计中重复运行，独立诊断结果另列如下。

### 新发现的待修项与测试解释

- 数据与复现：完整分类字段被当成缺失而误删；带引号内换行的 CSV 导入改变观测配对；稀疏 XLSX 后行超出表头宽度时越界；保存随机预设未保留当次数据。
- 数值计算：`1/x` 丢失分段信息后产生假零点；曲线求交受绝对容差和截断影响；大数 z-score 中间平方溢出。
- 绘图表达：原理化坐标仍破坏微小范围、截断第 6001 个点、错放相对注释；跨图色标无法锁定；分布图元没有真实计数尺度。
- 能力缺口：误差条/置信带、原生对数与时间轴、自动图例与多面板、拟合诊断、完整实验复现与批量执行。

独立数据工作流诊断按正确行为断言，结果 **1 个对照通过、3 个缺陷反例失败**，见 [原始日志](verification/audit-data-workflow.txt)。数值观察程序及 6 个绘图现状诊断记录了错误行为，运行成功不表示对应需求通过。它们位于 `examples/audit/` 和 `docs/audit/probes/`，不并入此前 111 项回归通过的统计，也没有通过放宽产品测试掩盖问题。

新增 [第六篇教学记录：测试覆盖与数据语义](learning/06-测试覆盖与数据语义.md)，包含测试集合与数学全称命题、数据往返不变量、随机性复现、介值定理条件和标准化溢出反例；数值审计另含尺度分析、误差条语义与练习。

## 2026-09-09 续记：Flutter SDK 路径整理

Flutter 3.47.2 / Dart 3.13.2 从工作区深层目录迁移到 `%USERPROFILE%\develop\flutter`。该目录位于用户配置文件下、无空格和特殊字符，也不需要写入 `Program Files` 等受保护位置；`bin` 已加入当前用户 PATH。旧的 `.syphon-tools/flutter` 目录已不存在，项目 Pub 缓存和独立上游审计副本仍保留在 `.syphon-tools`。

`scripts/verify.ps1` 现在依次使用显式参数或 `SYPHON_FLUTTER_ROOT`、用户目录 SDK、旧目录兼容项、系统 PATH，并独立发现工作区 Pub 缓存。迁移后实际执行 `./scripts/verify.ps1 -Build`：依赖解析成功、静态分析无问题、**111/111 测试通过**，Windows Release 构建成功，编译阶段报告 38.0 秒。PATH 与脚本检查见 [Flutter 迁移验证](verification/flutter-relocation-2026-09-09.md)。

同日复核上游 v0.3.6：新版自带 88 项测试全部通过，但本地原先四个数值反例仍 4/4 失败；小尺度线段求交已改善，其他多数正确性问题仍存在。完整对照见 [上游 v0.3.6 复核](audit/2026-09-09-upstream-v0.3.6.md)，教学推导见 [尺度自适应与精确几何](learning/07-尺度自适应与精确几何.md)。本地没有直接合并新版，以免覆盖现有科研修复。

